# Go to repo
cd /home/pk21271/msciproject

# Stage the commits
git add .
git status

# Commit them with message
git commit -m "EDITS"

# Pushes commits to the repo
git push origin main

cd /home/pk21271/msciproject
git add .
git commit -m "EDITS"
git push origin main
